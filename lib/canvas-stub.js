// @react-pdf/renderer optionally uses `canvas` for advanced image handling.
// We don't need that in this app, so this stub keeps bundlers from choking
// on the native `canvas` package, which isn't installed here.
module.exports = {};
